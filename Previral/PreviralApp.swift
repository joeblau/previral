import SwiftUI

@main
struct PreviralApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        .windowStyle(.titleBar)
        .defaultSize(width: 1280, height: 800)
    }
}
